help: Show current dhcp leases for a specific pool
run: sudo /opt/vyatta/sbin/command_proc_show_dhcp_leases show_dhcp_leases.xsl "$5"
allowed: ( [ -e /var/log/dhcpd.leases ] && grep '^#shared-network: ' /var/log/dhcpd.leases ) | perl -p -e 's/^#shared-network: $1/$1/' | sort -u
