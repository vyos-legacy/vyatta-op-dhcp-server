help: Show current DHCP statistics for a specific pool
run: /opt/vyatta/bin/sudo-users/vyatta-show-dhcp-server.pl --show-stats --pool "$5"
allowed: ( [ -e /var/log/dhcpd.leases ] && grep '^#shared-network: ' /var/log/dhcpd.leases ) | perl -p -e 's/^#shared-network: $1/$1/' | sort -u
