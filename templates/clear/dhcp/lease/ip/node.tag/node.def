help: Clear the DHCP lease for a specific IP address
run: sudo /opt/vyatta/bin/sudo-users/vyatta-clear-dhcp-lease.pl  --ilfile=/var/log/dhcpd.leases --olfile=/var/log/dhcpd.leases --lip="$5"  --pidf=/var/run/dhcpd.pid --init=/opt/vyatta/sbin/dhcpd.init
allowed: ( [ -e /var/log/dhcpd.leases ] && grep -E '^lease .+ {' /var/log/dhcpd.leases ) | perl -p -e 's/^lease (.+) {/$1/' | sort -u
